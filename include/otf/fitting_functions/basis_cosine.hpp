#ifndef MAVOR_COSINE_H
#define MAVOR_COSINE_H

/// @brief Evaluates the cosine basis function for a given order and input value.
/// @param x The input value.
/// @param order The order of the cosine basis function.
/// @return The value of the cosine basis function of the specified order at x.
double eval_cosine_order(double const & x, int const order);

#endif